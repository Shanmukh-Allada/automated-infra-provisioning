- name: Install essential packages on Red Hat EC2
  hosts: all
  become: yes

  tasks:
    - name: Install vim, curl, and git
      yum:
        name:
          - vim
          - curl
          - git
        state: present